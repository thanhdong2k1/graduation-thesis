import CardAuth from "../components/cards/CardAuth";

const Login = () => {
    return (
        // w-full
        <div className="loginDiv h-screen w-screen flex justify-center items-center overflow-hidden rounded-lg">
            <CardAuth />
        </div>
    );
};

export default Login;
